package com.lynhaw.g4test.enumeration;

/**
 * @Author yyhu3
 * @Date 2018-09-20 10:18
 */
public enum CommonEnum {
    //NEED:需要接口覆盖度 不需要代码覆盖度
    //NONEED:不需要接口覆盖度 不需要代码覆盖度
    //NEEDANDCOVER:需要接口覆盖度 需要代码覆盖度
    //NONEEDANDCOVER:不需要接口覆盖度 需要代码覆盖度
    NEED,NONEED,NEEDANDCOVER,NONEEDANDCOVER;
    }
